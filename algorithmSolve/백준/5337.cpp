#include <iostream>

using namespace std;

// 풀이.
// 특수문자 출력 방법을 아는지 묻는 문제다. ', ", \등은 일반적인 문자열로 출력할수 없고
// 그 앞에 \를 하나 붙여줘야 정상적으로 출력이 된다.

int main()
{
    cout << ".  .   ." << '\n';
    cout << "|  | _ | _. _ ._ _  _" << '\n';
    cout << "|/\\|(/.|(_.(_)[ | )(/." << '\n';
    return 0;
}